<script src="jquery.min.js"></script>
<script type="text/javascript">
$(document).ready(function(){
    $('#category').on('change',function(){
        var categoryID = $(this).val();
        if(categoryID){
            $.ajax({
                type:'POST',
                url:'ajaxData.php',
                data:'categoryy_id='+categoryID,
                success:function(html){
                    $('#Sub-category').html(html);
                    $('#company').html('<option value="">Select Sub-category first</option>'); 
                }
            }); 
        }else{
            $('#Sub-category').html('<option value="">Select category first</option>');
            $('#company').html('<option value="">Select Sub-category first</option>'); 
        }
    });
    
    $('#Sub-category').on('change',function(){
        var Sub-categoryID = $(this).val();
        if(Sub-categpryID){
            $.ajax({
                type:'POST',
                url:'ajaxData.php',
                data:'Sub-category_id='+Sub-categoryID,
                success:function(html){
                    $('#company').html(html);
                }
            }); 
        }else{
            $('#company').html('<option value="">Select Sub-category first</option>'); 
        }
    });
});
</script>
Using PHP & HTML the initial categort select box is displayed along with the Sub-category and company select box.

<?php
//Include database configuration file
include('dbConfig.php');

//Get all category data
$query = $db->query("SELECT * FROM category WHERE status = 1 ORDER BY categpry_name ASC");

//Count total number of rows
$rowCount = $query->num_rows;
?>
<select name="category" id="category">
    <option value="">Select category</option>
    <?php
    if($rowCount > 0){
        while($row = $query->fetch_assoc()){ 
            echo '<option value="'.$row['category_id'].'">'.$row['category_name'].'</option>';
        }
    }else{
        echo '<option value="">category not available</option>';
    }
    ?>
</select>

<select name="Sub-category" id="Sub-category">
    <option value="">Select category first</option>
</select>

<select name="company" id="company">
    <option value="">Select Sub-category first</option>
</select>
